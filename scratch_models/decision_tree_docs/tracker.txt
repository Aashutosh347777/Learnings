2026 May 14 
-> Initialized the repo
-> Tried creating a non linear functions something like (ax^5 + bx^4 + cx^3 + dx^2 + ex + f\) 

2026 May 15
-Created the data in the required formate
-Studied about the range of values being used to generate the data -> like changing the range of x from -100-100 to -5-5
-Also the current data config, may need to change to one consistent non linear function.

2026 May 28
-Feature split caluclation, try histogram based binding for candidate threshold caluclation

2026 Jun 1
-Information gain for each bin, comparison and finding out the best, splitting bin, and either its mean or upperbound.

2026 jun 4
-Pushing ill broken code.

2026 Jun 22
-Refactoring attempt, latex documentation incomplete, multi class computation attempt.  

2026 Jun 24
-Documented the split caluclation for discrete columns (binary splitting) and for continous calculation (greedy approach).

2026 Jun 25
-Working on fixing the probability calculation in refactoring (greedy split), same probability for each target class in traget column.

2026 Jul 3
-Fixed the probability calculation for greedy split and studying recursion apporach.

2026 Jul 7
-Figured out _cal_ig_col function is not working dynamically, need to change variable assesment local to global.

2026 Jul 9
-Wrong column number mapping in while returning mapping_splits in _cal_ig_col, the column number(col_num is a int taken out from range(num_cols), but when finding the col type for finding which kind of evaluation to run in find_best_split(split_dict and col_dtype), we're getting wrong col number.)
-tree_formation dictionary being overwritten because of the same col_num being passed as key.

2026 Jul 13
-Reached leaf node with correct data passage
-Sequence of column pop needed for the upward travel recursion, but not being found, make it dynamic.
-Proper Leafing needed(When a single column is reached nothing is being passed as feat_col_in)